apiVersion: v1
kind: Service
metadata:
  name: catalogues
  namespace: cec
  labels:
    app: catalogues
spec:
  ports:
    - port: 3333
      targetPort: 3333
      protocol: TCP
  selector:
    app: catalogues
