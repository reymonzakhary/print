apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogues
  namespace: cec
  labels:
    app: catalogues
spec:
  selector:
    matchLabels:
      app: catalogues
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: catalogues
    spec:
      containers:
        - name: catalogues
          image: <IMAGE>
          ports:
            - containerPort: 3333
              name: catalogues
          volumeMounts:
            - name: cec-assortment-config
              mountPath: /tmp/ca-certificate.crt
              subPath: ca-certificate.crt
              readOnly: true
            - name: cec-catalogues-env
              mountPath: /tmp/.env
              subPath: .env
          lifecycle:
            postStart:
              exec:
                command: ["/bin/sh", "-c", "cp -r /tmp/ca-certificate.crt /var/crt && cp -r /tmp/.env /var/www/.env"]
      volumes:
        - name: cec-assortment-config
          configMap:
            name: cec-assortment-config
        - name: cec-catalogues-env
          configMap:
            name: cec-catalogues-env
            items:
              - key: .env
                path: .env
