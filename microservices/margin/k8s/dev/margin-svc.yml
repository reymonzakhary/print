apiVersion: v1
kind: Service
metadata:
  name: margin
  namespace: cec
  labels:
    app: margin
spec:
  ports:
    - port: 3333
      targetPort: 3333
      protocol: TCP
  selector:
    app: margin
