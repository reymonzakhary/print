from resources.interface.calculationInterface import calculationInterface


class fullCalculation(calculationInterface):
    def netPrice(self, category, options,qty,addonsOptions) -> int:
        # Make Calculations
        return 50

