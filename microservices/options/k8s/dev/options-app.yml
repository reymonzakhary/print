apiVersion: apps/v1
kind: Deployment
metadata:
  name: options
  namespace: cec
  labels:
    app: options
spec:
  selector:
    matchLabels:
      app: options
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: options
    spec:
      containers:
        - name: options
          image: <IMAGE>
          ports:
            - containerPort: 3333
              name: options
          volumeMounts:
            - name: cec-assortment-config
              mountPath: /tmp/ca-certificate.crt
              subPath: ca-certificate.crt
              readOnly: true
            - name: cec-options-env
              mountPath: /tmp/.env
              subPath: .env
          lifecycle:
            postStart:
              exec:
                command: ["/bin/sh", "-c", "cp -r /tmp/ca-certificate.crt /var/crt && cp -r /tmp/.env /var/www/.env"]
      volumes:
        - name: cec-assortment-config
          configMap:
            name: cec-assortment-config
        - name: cec-options-env
          configMap:
            name: cec-options-env
            items:
              - key: .env
                path: .env
