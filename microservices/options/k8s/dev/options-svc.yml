apiVersion: v1
kind: Service
metadata:
  name: options
  namespace: cec
  labels:
    app: options
spec:
  ports:
    - port: 3333
      targetPort: 3333
      protocol: TCP
  selector:
    app: options
