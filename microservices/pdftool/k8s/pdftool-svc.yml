apiVersion: v1
kind: Service
metadata:
  name: pdftool
  namespace: cec
  labels:
    app: pdftool
spec:
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: pdftool
