apiVersion: v1
kind: Service
metadata:
  name: calculation
  namespace: cec
  labels:
    app: calculation
spec:
  ports:
    - port: 3333
      targetPort: 3333
      protocol: TCP
  selector:
    app: calculation
