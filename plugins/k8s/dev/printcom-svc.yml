apiVersion: v1
kind: Service
metadata:
  name: printcom
  namespace: cec
  labels:
    app: printcom
spec:
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: printcom
