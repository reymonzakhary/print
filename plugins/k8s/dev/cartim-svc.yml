apiVersion: v1
kind: Service
metadata:
  name: cartim
  namespace: cec
  labels:
    app: cartim
spec:
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: cartim
