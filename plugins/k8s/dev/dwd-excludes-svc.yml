apiVersion: v1
kind: Service
metadata:
  name: dwd-exclude
  namespace: cec
  labels:
    app: dwd-exclude
spec:
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: dwd-exclude
